1. A client has sent a POST request to our REST API containing data to create a new quote in our datastore. What does the Express.json() middleware do?

- A and B

2. How do we catch errors when using async/await syntax?

- Use "try...catch"

3. What do status codes in the 400 range mean?

- The request has failed

4. Which middleware method parses incoming JSON from the client and makes it available to our Express server via req.body?

- Express.json()

5. What do status codes in the 20 range mean?

- The request has succeeded
