1. The `get` method takes two parameters _ and _.

- path, callback

2. A route is where a server is set up to respond to a \_ from a client.

- request

3. In express, the convention for creating a new application, is to assign it to the variable _: (e.g. const _ = express();)

- app

4. The `app.listen` method can take a parameter, which will tell the server:

- which port to serve the application on

5. The `get` method registers a route that will listen for HTTP \_ requests at a given route.

- GET
