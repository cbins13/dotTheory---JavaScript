extends layout.pug

block content
    section#content
        if colors
            ul
                each color in colors
                    li= color
            h2= prompt
        if hint
            p 
                i Hint: #{hint}